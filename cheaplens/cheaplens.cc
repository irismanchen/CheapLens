#include "opencv2/opencv.hpp"

#include "cheaplens.hpp"

int main(int args, char** argv)
{
    // Setup camera.
    //VideoCapture cv::cap(2);
    //if(!cap.isOpened())
    //    return -1;

    // Calibration.
    // Pose.
    // Render.
    return 0;
}
